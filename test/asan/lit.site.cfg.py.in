@LIT_SITE_CFG_IN_HEADER@

# Tool-specific config options.
config.name_suffix = "@XSAN_TEST_CONFIG_SUFFIX@"
config.target_cflags = "@XSAN_TEST_TARGET_CFLAGS@"
config.clang = "@XSAN_TEST_TARGET_CC@"
config.bits = "@XSAN_TEST_BITS@"
config.arm_thumb = "@COMPILER_RT_ARM_THUMB@"
config.apple_platform = "@XSAN_TEST_APPLE_PLATFORM@"
config.apple_platform_min_deployment_target_flag = "@XSAN_TEST_MIN_DEPLOYMENT_TARGET_FLAG@"
config.xsan_dynamic = @XSAN_TEST_DYNAMIC@
config.target_arch = "@XSAN_TEST_TARGET_ARCH@"

# Load common config for all compiler-rt lit tests.
lit_config.load_config(config, "@XSAN_COMMON_LIT_CONFIGURE_PATH@")

# Load tool-specific config that would do the real work.
lit_config.load_config(config, "@ASAN_TEST_SOURCE_DIR@/lit.cfg.py")
